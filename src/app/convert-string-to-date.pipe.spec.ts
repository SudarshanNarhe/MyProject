import { ConvertStringToDatePipe } from './convert-string-to-date.pipe';

describe('ConvertStringToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertStringToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
